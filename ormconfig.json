{
  "type": "mongodb",
  "host": "localhost",
  "port": 27017,
  "database": "circle-fullstack",
  "synchronize": true,
  "logging": "all",
  "cache": true,
  "dropSchema": "true",
  "logger": "file",
  "entities": ["src/entity/**/*.{ts,js}"],
  "migrations": ["src/migration/**/*.{ts,js}"],
  "subscribers": ["serversrc/subscriber/**/*.{ts,js}"]
}
